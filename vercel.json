{
  "buildCommand": "echo 'no build needed'",
  "outputDirectory": "public",
  "functions": {
    "api/cron.js": { "maxDuration": 10 },
    "api/scan.js": { "maxDuration": 10 }
  },
  "rewrites": [
    { "source": "/api/keepalive", "destination": "/api/keep-alive" }
  ]
}